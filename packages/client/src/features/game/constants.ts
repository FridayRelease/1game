enum SIDES {
  TOP = 'top',
  BOTTOM = 'bottom',
  LEFT = 'left',
  RIGHT = 'right',
}

enum POSITION {
  NONE = 0,
  FRIEND = 1,
  VILLAIN = 2,
}

enum EntityType {
  NONE,
  TANK,
  BULLET,
}

export { SIDES, POSITION, EntityType };
